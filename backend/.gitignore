# Dependencies
/node_modules

# Environment variables (Critical if .env is inside the backend folder)
.env
.env.*
.env.local

# Runtime data
npm-debug.log*
yarn-debug.log*

# Production / Build artifacts
/dist
/build